<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Profile</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
  <div class="w-full min-h-screen bg-zinc-900 flex text-white flex-col gap-4 p-10">
    <h3 class="text-zinc-500 text-xl ">Edit your Post here</h3>
    <form class="mb-4" action="/update/<%= post._id %>" method="post">
      <textarea class="w-1/3 text-white resize-none bg-transparent border-2 border-zinc-700 rounded-lg p-2 outline-none"
        name="content" id="" cols="30" rows="10" placeholder="Whats on your mind ?"><%= post.content %></textarea>
      <button class="bg-yellow-500 text-black px-4 py-2 rounded-lg block" type="submit">Update Post</button>
    </form>
  </div>
</body>

</html>