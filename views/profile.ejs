<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 flex text-white flex-col gap-4 p-10">
      <div class="flex items-center gap-4">
        <img class="w-20 h-20 rounded-full object-cover" src="/img/uploads/<%= user.profilePic %>" alt="profile pic">
        <h1 class="text-2xl font-bold">Hey, <%= user.name %></h1>
      </div>
      <button class="w-[200px] bg-blue-500 text-white px-2 py-2 rounded-lg" type="button" onclick="window.location.href='/profile/upload'">Change Profile Picture</button>
      <p><%= user.username %></p>
      <p><%= user.email %></p>
      <p><%= user.age %></p>
      <form class="mb-4" action="/post" method="post">
        <textarea class="w-1/3 text-white resize-none bg-transparent border-2 border-zinc-700 rounded-lg p-2 outline-none" name="content" id="" cols="30" rows="10" placeholder="Whats on your mind ?"></textarea>
        <button class="bg-blue-500 text-white px-4 py-2 rounded-lg block" type="submit">Create new Post</button>
      </form>
      <h3 class="text-zinc-500 text-xl ">Your Posts</h3>
      <div class="post-container flex flex-wrap gap-4">
        <% user.posts.forEach((post) => { %>
        <div class="post w-1/3 bg-zinc-800 rounded-lg p-2 border-1 border-zinc-600">
          <h4 class="text-blue-500 text-sm">@<%= user.username %></h4>
          <p class="text-sm tracking-tight"><%= post.content %> </p>
          <p class="text-sm text-zinc-500"><%= post.likes.length %> likes</p>
          <div class="flex gap-2 mt-2">
            <a href="/like/<%= post._id %>" class="bg-transparent text-blue-500 text-sm rounded-lg">
              <%= post.likes.includes(user._id) ? "Unlike" : "Like" %>
            </a>
            <a href="/edit/<%= post._id %>" class="bg-transparent text-zinc-500 text-sm rounded-lg">Edit</a>
          </div>
        </div>
        <% }) %>
      </div>

    </div>
</body>
</html>